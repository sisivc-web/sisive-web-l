export const homeData = state => state.homeData

export const currentUser = state => state.currentUser

export const contrastList = state => state.contrastList

export const showMessaging = state => state.showMessaging

export const participant = state => state.participant
